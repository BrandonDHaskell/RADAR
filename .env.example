# Postgres connection (required). Matches docker-compose.yml defaults.
DATABASE_URL=postgres://radar:radar_dev_password@localhost:5432/radar?sslmode=disable

# Voyage AI embeddings (required starting Phase 3: profile and posting embeddings)
VOYAGE_API_KEY=

# Anthropic Claude API (required starting Phase 4: LLM fit verdicts)
ANTHROPIC_API_KEY=
